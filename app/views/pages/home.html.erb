<div class="banner banner--home">
  <div class="container">
    <h1>Tes prochains potes de rando sont là</h1>
    <p>la co-randonnée en toute simplicité !</p>
      <p>
        <%= render "shared/search-home" %>
      </p>
  </div>
</div>

<section class="trip__presentation">
  <div class="container">
    <div class="row">

      <% @trips.each do |trip| %>
        <div class="col-lg-6">
           <%= render "trips/trip-card", trip: trip %>
        </div>
      <% end %>

    </div>
  </div>
</section>
