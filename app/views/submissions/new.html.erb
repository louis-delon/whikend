<h1>Submit your application</h1>



<div>
  <p><strong> <%= @trip.start_location %></strong></p>
  <p> <%= @trip.description %></p>

  <%= simple_form_for [@trip, @submission] do |f| %>
    <%= f.input :content %>
    <%= f.submit "Submit", class: "btn btn--primary" %>
  <% end %>
</div>
