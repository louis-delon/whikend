<%= simple_form_for [trip, message], remote: true do |f| %>
  <%= f.input :content, placeholder: "You can ask questions or just say hi to your buddies!" %>
  <%= button_tag class: "btn btn--secondary" do %>
    Send your message<%= icon('fas', 'comments') %>
  <% end %>
<% end %>
