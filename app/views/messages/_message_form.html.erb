<%= simple_form_for [trip, message], remote: true do |f| %>
  <%= f.input :content, placeholder: "Vous pouvez interagir avec les participants de la sortie!" %>
  <%= button_tag class: "btn btn--secondary" do %>
    Envoyer votre message<%= icon('fas', 'comments') %>
  <% end %>
<% end %>
